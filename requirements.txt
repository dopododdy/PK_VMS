# VASD Agent v6 – Python dependencies
# Install with: pip install -r requirements.txt

flask>=2.3
flask-cors>=4.0
pyscard>=2.0          # Thai ID card smartcard reader (PCSC)
pdfkit>=1.0           # HTML → PDF via wkhtmltopdf (install wkhtmltopdf separately)

# Windows-only (install manually on Windows):
# pip install pywin32
